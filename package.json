{
  "name": "mondoo-ui-exam",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "codegen": "pnpm --filter client codegen"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
